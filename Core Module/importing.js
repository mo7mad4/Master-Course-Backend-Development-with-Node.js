// this's core Module and import all functions in fs 
const fs = require("fs")
//this's loacl Module 
// const fs = require("./fs")

// this's readFile function in fs just
const { readFile } = "fs"


fs.readFile("path", "utf-8" , (err, data)=>{

})

readFile("path", "utf-8" , (err, data)=>{

})